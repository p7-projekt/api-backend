meta {
  name: JoinSession
  type: http
  seq: 3
}

post {
  url: {{baseURL}}/join
  body: json
  auth: none
}

body:json {
  {
      "SessionId": {{sessionId}},
      "SessionCode": "{{sessionCode}}"
  }
}

script:post-response {
  bru.setEnvVar("anonToken",res.body.token);
}
