meta {
  name: GetSolution
  type: http
  seq: 3
}

get {
  url: {{baseURL}}/{{currentVersion}}/dashboard/solution/{{exerciseId}}/{{appUserId}}
  body: none
  auth: bearer
}

auth:bearer {
  token: {{instructorToken}}
}
