meta {
  name: GetSolution
  type: http
  seq: 3
}

get {
  url: {{baseURL}}/{{currentVersion}}/dashboard/solution/{{exerciseId}}/{{userId}}
  body: none
  auth: bearer
}

auth:bearer {
  token: {{instructorToken}}
}
