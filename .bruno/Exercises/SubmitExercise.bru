meta {
  name: SubmitExercise
  type: http
  seq: 2
}

post {
  url: {{baseURL}}/{{currentVersion}}/exercises/2/submission
  body: json
  auth: bearer
}

auth:bearer {
  token: {{anonToken}}
}

body:json {
  {
    "sessionId": 2,
    "solution": "solution x = if x < 0 then x * (-1) else x\n"
  }
}
