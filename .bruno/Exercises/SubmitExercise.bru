meta {
  name: SubmitExercise
  type: http
  seq: 2
}

post {
  url: {{baseURL}}/{{currentVersion}}/exercises/1/submission
  body: json
  auth: bearer
}

auth:bearer {
  token: {{anonToken}}
}

body:json {
  {
    "sessionId": 1,
    "solution": "solution x = if x < 0 then x * (-1) else x\n"
  }
}
