meta {
  name: SubmitExercise
  type: http
  seq: 2
}

post {
  url: {{baseURL}}/{{currentVersion}}/exercises/1/submit
  body: json
  auth: none
}

body:json {
  {
    "exerciseId": 1,
    "solution": "solution x = if x < 0 then x * (-1) else x\n"
  }
}
