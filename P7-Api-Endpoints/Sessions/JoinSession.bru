meta {
  name: JoinSession
  type: http
  seq: 3
}

post {
  url: {{baseURL}}/{{currentVersion}}/sessions/{{sessionId}}/participants
  body: json
  auth: none
}

body:json {
  {
      "SessionCode": "{{sessionCode}}"
  }
}

script:post-response {
  bru.setEnvVar("anonToken",res.body.token);
}
